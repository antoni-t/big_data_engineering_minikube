apiVersion: v1
kind: Service
metadata:
  name: heatmap
  namespace: default
spec:
  type: NodePort
  selector:
    app: heatmap
  ports:
    - name: http
      port: 8888
      targetPort: 8888
      nodePort: 30088   # optional: oder weglassen für auto
