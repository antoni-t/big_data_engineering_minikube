apiVersion: v1
kind: Service
metadata:
  name: machine-learning-model
  namespace: default
spec:
  type: NodePort
  selector:
    app: machine-learning-model
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 30089
